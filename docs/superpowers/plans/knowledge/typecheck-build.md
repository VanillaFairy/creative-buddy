# Typecheck and build

```
npx tsc --noEmit
npm run build
```

First command is a fast typecheck with no output files. Second one runs the same typecheck and then produces the esbuild production bundle (`main.js`).
