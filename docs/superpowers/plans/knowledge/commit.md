# Commit message format

```
type: subject

optional body

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

`type` is one of feat/test/chore/fix/docs. Body is optional. The trailer line is not — always include it. Never commit with failing tests.
